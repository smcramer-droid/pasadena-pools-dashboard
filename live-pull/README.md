# Live-site pull snapshot — 2026-04-23

Fetched via HTTP from: **pasadena-pools.pages.dev**

These files were recovered from the deployed Cloudflare Pages site (the server
had no source-download API). They're the client-side bundle as served to
browsers — complete for pages that are HTML-only, but any server-side Pages
`functions/` logic is NOT here (that only exists on Cloudflare's side).

This is a dated snapshot. The real source of truth for this tool, once it's
established, should live in the root of this repo alongside wrangler.toml.
